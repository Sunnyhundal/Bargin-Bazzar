<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <!-- External JS -->
    <script src="/scripts/edit-item.js"></script>

    <!-- App CSS -->
    <link rel="stylesheet" href="/styles/edit-item.css" type="text/css" />

    <title>Bargain Bazaar | Edit items</title>
    <style>
        .button-container {
            display: flex;
            gap: 1rem;
        }
    </style>
</head>
<body>
    <%- include('./partials/_back'); %>
    <%- include('./partials/_footer') %>
    <%- include('./partials/_headern'); %>
    <%- include('./partials/_navibar'); %>
    

    <div class="item-container">
        <h1>Edit Item</h1>
        <form method="post" action="/items/<%= item.id %>">
            <label for="title">Title:</label>
            <input type="text" id="title" name="title" value="<%= item.title %>" required>
            <br>
            <label for="price">Price:</label>
            <input type="number" id="price" name="price" value="<%= item.price %>" required>
            <br>
            <label for="description">Description:</label>
            <textarea id="description" name="description" rows="4" required><%= item.description %></textarea>
            <br>
            <label for="photo_url">Photo URL:</label>
            <input type="url" id="photo_url" name="photo_url" value="<%= item.photo_url %>" required>
            <br>
            <label for="thumbnail_url">Thumbnail URL:</label>
            <input type="url" id="thumbnail_url" name="thumbnail_url" value="<%= item.thumbnail_url %>" required>
            <br>
            <div class="button-container">
                <button type="submit">Save Changes</button>
                <a href="/" class="btn btn-primary">Back To Featured Items</a>
                <button type="button" id="deleteBtn" class="btn btn-danger">Delete Listing</button>
            </div>
        </form>
    </div>
        <!-- External JS with the defer attribute -->
        <script src="/public/scripts/edit-item.js" defer></script>

        
</body>
</html>

